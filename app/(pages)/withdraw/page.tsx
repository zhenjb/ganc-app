// Temporary placeholder — owned by a later FE task (Withdraw page). Replace
// with real implementation when that task is implemented. Required so the
// route compiles as a module under the App Router during FE-01.
export default function WithdrawPage() {
  return null;
}
