// Temporary placeholder — owned by a later FE task (Submit Proof page).
// Replace with real implementation when that task is implemented. Required
// so the route compiles as a module under the App Router during FE-01.
export default function SubmitProofPage() {
  return null;
}
