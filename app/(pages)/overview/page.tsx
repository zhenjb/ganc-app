// Temporary placeholder — owned by FE-02 (Overview page). Replace with real
// implementation when FE-02 is implemented. Required so the route compiles
// as a module under the App Router during FE-01.
export default function OverviewPage() {
  return null;
}
