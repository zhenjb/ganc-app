// Page-private types for the trade screen.
// Domain types live in @/app/lib/interfaces/trade.ts — this file is for
// view-layer only types that don't belong in the shared interfaces barrel.

export type TradeTab = "open" | "history";
